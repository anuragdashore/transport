<nav class="navbar navbar-expand-lg" style="background-color: #21D192;">
  <div class="container-fluid">
    <div class="logo">
      <img src="<%= asset_path("kalpvriksha.png") %>" width="14%" height="50vh">
      <a class="navbar-brand mb-0 h1" href="<%= root_path %>">Kalpvriksha Transportation</a>
    </div>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home", root_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "What we do", about_us_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Services", service_path, class:"nav-link" %>
        </li>
        <li class="nav-item"> 
          <%= link_to "Contact us", contact_us_path, class:"nav-link" %>  
        </li>
      </ul>
      <div>
        <% if user_signed_in? %>
          <%#= link_to "Your Bookings", bookings_path(user_id: current_user.id), class: "btn btn-primary me-3" %>
          <%= link_to "New Booking", new_booking_path(user_id: current_user.id), class: "btn btn-primary me-3" %>

          <% if current_user.has_role? :manager %>
            <%= link_to "All Users", users_path, class: "btn btn-primary me-3" %>
          <% end %>

          <div class="btn-group me-3">
            <button class="btn btn-dark dropdown-toggle bi bi-person-fill" type="button" data-bs-toggle="dropdown" aria-expanded="false"></i>
            your Profile</button>
            <ul class="dropdown-menu dropdown-menu-dark">
              <li><%= link_to "Profile", user_path(current_user), class:"dropdown-item" %></li>
              <li><%= link_to "Edit Your Profile", edit_user_path(current_user), class:"dropdown-item" %></li>
              <li><%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: :"Are You Sure?"}, class:"dropdown-item" %></li>
            </ul>
          </div>
        <% else %>
          <%= link_to "Login", new_user_session_path, class: "btn btn-outline-dark me-2" %>
          <%= link_to "Register", new_user_registration_path, class: "btn btn-outline-dark me-4" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>